export default function () {
  const _map = {
    "sort-icon":
      "invisible ml-2 flex-none rounded text-gray-400 group-hover:visible group-focus:visible",
    "sort-icon-sortable": "text-gray-900 !visible",
  };

  return _map;
}
